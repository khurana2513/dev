{
  "scripts": {
    "build":        "cd frontend && npm run build",
    "cap:sync":     "cd frontend && npm run build && cd .. && npx cap sync android",
    "cap:open":     "npx cap open android",
    "cap:run":      "cd frontend && npm run build && cd .. && npx cap sync android && npx cap run android",
    "cap:copy":     "npx cap copy android",
    "pwa:preview":  "cd frontend && npm run build && npm run preview"
  },
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@codetrix-studio/capacitor-google-auth": "^3.4.0-rc.4",
    "@vercel/speed-insights": "^1.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.2.0"
  }
}
