[build]
builder = "NIXPACKS"
buildCommand = "python3.12 -m pip install -r backend/requirements.txt && python3.12 -m playwright install chromium"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
